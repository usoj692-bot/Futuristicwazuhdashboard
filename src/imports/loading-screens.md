Update the landing/loading screens in the UI while keeping the current design style, colors, and layout unchanged. Only apply the changes mentioned below.

1. After Sign-In Page

Remove the existing landing/loading page that currently appears after sign-in.

Replace it with two sequential loading screens.

Screen 1
Text: “Welcome Back, Commander”

The text should appear with a typewriter animation (letter-by-letter effect).
Example: W → We → Wel → Welc → Welcome…

Leave proper spacing below the text.

After the sentence finishes typing, show five animated loading dots.

Screen 2
Text: “Your Dashboard Is Loading”

This text should also appear using the same typewriter animation (letter-by-letter).

Leave proper spacing below the text.

After the text completes, show five animated loading dots.

After this screen finishes, the final dashboard should load.

2. Planet / Module Landing Screens

Apply the same behavior to all planets/modules (such as FIM and other modules).

Remove the first landing page that currently shows blue text like “Initializing FIM Module” or similar module-specific messages.

This screen should no longer appear for any planet/module.

3. Planet Loading Sequence

For every planet/module, keep only the following loading messages:

Initializing…
Analyzing…
Preparing Your Dashboard…

Each message should:

Appear using a typewriter (letter-by-letter) animation

Have five animated loading dots displayed after the text

Maintain proper spacing between the text and the dots

Important

Do not modify the existing UI layout, colors, or theme.

Apply this loading behavior consistently across all planets/modules.

Maintain clean spacing and alignment.