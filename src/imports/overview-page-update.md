Update the **Overview page of my existing security platform UI**. The changes must follow the current design system already used in the project.

IMPORTANT RULES

• Do NOT redesign the UI
• Keep the same font family used across the platform
• Maintain consistent font sizes across all pages
• Use the same cards, spacing, border radius, and color system already present
• Do not modify the **Uniform Log Management** and **Predictive Analysis** section designs (only positioning if needed)
• Make all overview section cards **equal in size** so the dashboard looks clean and balanced
• Apply the **same background used on the Planet pages** to the Overview page

The Overview page should look **clean, futuristic, animated, and balanced**.

---

AGENTS SUMMARY

Replace the current layout with a **dynamic bar graph**.

The graph should display:

Connected
Disconnected
Partially Connected

Requirements:

• Use a **horizontal or vertical bar chart**
• Each bar should represent the count of agents
• Colors:

* Connected → Green
* Disconnected → Red
* Partially Connected → Yellow or Orange
  • Add smooth animation when the bars load
  • Display the count value on each bar
  • Keep the font size normal and consistent with the rest of the UI

---

SEVERITY SECTION

Keep the four severity cards:

Critical
High
Medium
Low

Changes required:

• **Reduce the numerical font size** for the severity counts
• Keep font readable and consistent with the rest of the UI
• Maintain color indicators for each severity
• Keep the same card design

---

FILE INTEGRITY MONITORING (FIM)

Modify the FIM table.

Add **one additional log entry** to the table.

Final table columns:

Timestamp
Source IP
Event
Path

Design adjustments:

• Reduce the **overall font size of the FIM table**
• Ensure **all table columns use the same font size**
• Keep typography consistent across all rows
• Reduce padding slightly so the table appears more compact
• Maintain clean row alignment and separators

---

FIM AND UNIFORM LOG MANAGEMENT LAYOUT

Position the sections so that:

FIM section
and
Uniform Log Management section

appear **on the same row (same lane)**.

Both cards should be **equal in width and height**.

---

MITRE SECTION

Modify the table structure.

Replace the **Agent Name column** with:

CVE

Final columns:

CVE
Rule Name
Severity

Keep the same table style used in other sections.

---

CARD SIZE CONSISTENCY

Ensure that **all overview section cards are equal in size**, including:

Agents Summary
Severity
Uniform Log Management
File Integrity Monitoring
Predictive Analysis
MITRE

Cards should align in a **balanced grid layout** so the dashboard looks visually organized.

---

BACKGROUND

Use the **same background used in the Planet pages** for the Overview page.

Do not create a new background style.

---

FONT CONSISTENCY

Ensure that:

• All pages use the **same font family**
• Typography scale remains consistent
• Table fonts, card fonts, and titles use a **uniform style**

---

UNCHANGED SECTIONS

Do NOT modify the structure or design of:

Uniform Log Management
Predictive Analysis

Only ensure they align properly within the grid.

---

FINAL RESULT

The Overview page should appear as a **clean consolidated dashboard** with:

Balanced cards
Consistent typography
Planet page background
Compact tables
Animated bar chart for agents
Aligned sections

The final UI should feel **futuristic, smooth, and visually organized**.
