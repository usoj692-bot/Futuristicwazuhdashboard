Modify ONLY the Sign-In page of the current UI.

Do NOT change any other pages, dashboards, modules, planets, layouts, or navigation components.

Keep the existing black background with twinkling stars exactly the same.

Redesign only the sign-in interface to make it look more futuristic, dynamic, and cinematic, similar to a high-tech cybersecurity command console.

Layout

Create a vertical glassmorphism login panel positioned in the center of the screen so the star background remains clearly visible around it.

The panel must be semi-transparent, allowing the starry background to be seen through the panel.

Panel Design

Panel style:

Semi-transparent glass panel

Subtle backdrop blur effect

Soft neon glowing border

Rounded corners (around 18–22px)

Example styling:

Background:
rgba(10, 20, 40, 0.35)

Backdrop blur:
20–30px

Border glow:
Electric cyan or violet

Header Section

At the top of the panel add a futuristic security shield icon.

Below the icon place the heading:

SOC Command Center

Subheading below it:

Secure Access Portal

Use a soft glowing gradient text style (cyan to violet).

Login Fields

Add the following input fields:

Username
Password

Design the inputs like futuristic cyber terminal fields with subtle glowing edges.

Add small icons:

Username → user or network node icon
Password → eye visibility toggle icon

Additional Elements

Below the fields add:

Remember Me toggle switch

Sign-In Button

Create a futuristic neon activation button for:

Sign In

Style:

glowing gradient

cyan or neon green

soft hover animation

subtle glow ripple effect

Security Status Text

At the bottom of the panel add small system status text:

Secure Connection Active
256-bit Encryption

This should appear subtle and technical, like a system console indicator.

Background Enhancement

Keep the existing starry space background exactly as it is, but add a very faint futuristic grid overlay behind the login panel.

The grid should be:

extremely subtle

around 5–10% opacity

thin lines

resembling a SOC radar or command interface grid

This should enhance the futuristic feel without overpowering the star background.

Animations

Add subtle UI animations only to the sign-in panel:

soft neon glow pulse on panel border

input field glow when hovered or focused

button glow animation on hover

Animations should remain smooth, minimal, and professional.

Important Rules

Modify ONLY the sign-in page

Keep all other UI screens unchanged

Preserve the existing star background

Maintain a futuristic SOC command center style